name: Download Memes From Reddit

on:
  schedule:
    - cron: '0 * * * *' # Runs every hour
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  download-memes:
    name: 🌐 Download Memes
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🧰 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade git+https://github.com/praw-dev/praw.git
          pip install requests

      - name: 🔄 Restore Cache
        id: cache-restore
        uses: actions/cache@v4
        with:
          path: cache/memes.json
          key: reddit-memes-cache

      - name: ⏬ Download Latest Memes
        id: download-memes
        env:
          CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
          USER_AGENT: ${{ secrets.REDDIT_USER_AGENT }}
        run: |
          mkdir -p cache
          touch cache/memes.json
          python - <<EOF
          import os
          import json
          import requests
          import praw
          
          # Environment variables for credentials
          client_id = os.getenv("CLIENT_ID")
          client_secret = os.getenv("CLIENT_SECRET")
          user_agent = os.getenv("USER_AGENT")
          
          print("🔧 DEBUG: Initializing Reddit client in read-only mode...")
          reddit = praw.Reddit(
              client_id=client_id,
              client_secret=client_secret,
              user_agent=user_agent,
          )
          
          # Load cached URLs
          cache_file = "cache/memes.json"
          print(f"🔧 DEBUG: Loading cached URLs from {cache_file}...")
          try:
              with open(cache_file, "r") as f:
                  cached_urls = set(json.load(f))
              print(f"🔧 DEBUG: {len(cached_urls)} cached URLs loaded.")
          except (FileNotFoundError, json.JSONDecodeError) as e:
              print(f"⚠️ DEBUG: Cache file not found or invalid. Starting with an empty cache. Error: {e}")
              cached_urls = set()
          
          # Create a directory for images
          image_dir = "cache"
          os.makedirs(image_dir, exist_ok=True)
          print(f"🔧 DEBUG: Ensured image directory exists at {image_dir}.")
          
          # Fetch new posts from r/memes
          print("🔧 DEBUG: Fetching latest posts from r/memes...")
          new_urls = []
          for post in reddit.subreddit("memes").new(limit=10):
              print(f"🔧 DEBUG: Processing post: {post.title} | URL: {post.url}")
              
              if post.url not in cached_urls:
                  try:
                      print("🔧 DEBUG: Sending HEAD request to validate content type...")
                      response = requests.head(post.url, allow_redirects=True, timeout=5)
                      content_type = response.headers.get("Content-Type", "")
                      print(f"🔧 DEBUG: Content-Type for {post.url} is {content_type}.")
          
                      if content_type.startswith("image/"):
                          print(f"🎉 New Meme Found:\n  Title: {post.title}\n  Author: u/{post.author}\n  Upvotes: {post.score}\n  URL: {post.url}")
                          
                          # Save the image
                          print(f"🔧 DEBUG: Sending GET request to download the image...")
                          response = requests.get(post.url, timeout=10)
                          filename = os.path.join(image_dir, os.path.basename(post.url))
                          with open(filename, "wb") as f:
                              f.write(response.content)
                          print(f"✅ Image saved to {filename}.")
                          new_urls.append(post.url)
                      else:
                          print(f"⚠️ Skipping Non-Image URL: {post.url}")
                  except requests.RequestException as e:
                      print(f"❌ Failed to Process URL {post.url}. Error: {e}")
              else:
                  print(f"⚠️ DEBUG: URL already cached: {post.url}")
          
          # Update cache
          print(f"🔧 DEBUG: Updating cache with {len(new_urls)} new URLs...")
          cached_urls.update(new_urls)
          with open(cache_file, "w") as f:
              json.dump(list(cached_urls), f)
          print("✅ Cache updated.")
          
          if not new_urls:
              print("✨ No New Memes Found!")
          else:
              print(f"🎉 {len(new_urls)} new memes downloaded.")
          EOF

      - name: 📜 List All Files
        run: |
          tree

      - name: 🛠️ Save Artifacts
        if: steps.download-memes.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: memes
          path: cache/*.png,cache/*.jpg,cache/*.jpeg

      - name: 🧹 Update Cache
        if: steps.download-memes.outcome == 'success'
        uses: actions/cache@v4
        with:
          path: cache/memes.json
          key: reddit-memes-cache
